import { View, Text } from 'react-native';

/** Global app settings screen. Implemented in Phase 7. */
export default function SettingsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-lg font-semibold text-gray-600">Settings — coming in Phase 7</Text>
    </View>
  );
}
