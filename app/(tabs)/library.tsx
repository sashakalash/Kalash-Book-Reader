import { View, Text } from 'react-native';

/** Main library screen — book grid. Implemented in Phase 4. */
export default function LibraryScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-lg font-semibold text-gray-600">Library — coming in Phase 4</Text>
    </View>
  );
}
